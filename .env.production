# Production Environment Configuration
# Este archivo se usa cuando ejecutas: npm run build

# API Configuration - Google Cloud Run
# IMPORTANTE: Usar string vacío para URLs relativas (frontend y backend en mismo dominio)
# Empty string = relative URLs (same domain as frontend)
VITE_API_BASE_URL=""

# Application Settings
NODE_ENV=production

# Security Settings (no exponer secretos aquí, esto es solo frontend)
# Los secretos reales deben estar en Cloud Run para el backend
